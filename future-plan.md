# Plans for the future

1. Make a separate folder called leak_reports/ which contains leak_report.json files
    - When "Generate Leak Report" is clicked, the generated leak report will have a special UUID (which will be displayed in the CLI GUI).
    - Then the leak report will be saved as leak_reports/leak_report_UUID.json.
    - Leak Report json file should also provide the date and time at which the report was generated at the beginning.
